const Navbar = () => {
    return (
        <div>
            <nav>
                <a href="#" >Home</a>
                <a href="#">About</a>
                <a href="#">Profiles</a>
            </nav>
        </div>
    );
}
export default Navbar;